export const PRICE_RANGE = {
  MIN: 0,
  MAX: 1000,
  STEP: 10
};

export const RATING_RANGE = {
  MIN: 0,
  MAX: 5,
  STEP: 0.5
};

export const DEFAULT_FILTERS = {
  minPrice: PRICE_RANGE.MIN,
  maxPrice: PRICE_RANGE.MAX,
  minRating: RATING_RANGE.MIN
};